module Foo exposing (add, multiply, subtract)

add : Int -> Int -> Int
add a b =
    a + b

multiply : Int -> Int -> Int
multiply a b =
    a * b

subtract : Int -> Int -> Int
subtract a b =
    a - b
